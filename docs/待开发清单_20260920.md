# 经营分析平台待开发清单

更新时间：2026-09-20  
当前版本：`v0.7.0-import-workbench`

## 一、当前已完成

### 1. 前端 Demo

- 一套平台、基地/产品线/事业部三套视角切换。
- 经营总览、损益结果、指标树、PVM、AI经营分析、数据导入、映射配置、指标库、用户管理、菜单管理、权限验证等页面。
- 角色切换、基地范围切换、菜单配置、指标展示配置等演示交互。
- 基地损益表和产品线/事业部损益表按不同报表结构展示。
- 毛利分析支持产品类型、型号、客户项目、客户简称多维组合。
- Demo 已按财经反馈补充损益项目、PVM指标库、AI简析及配置入口。

### 2. 后端基础能力

- JDK8 + Spring Boot 2.7 模块化单体结构。
- 三视角领域模型、分析批次、批次状态流转。
- 月度数据集目录和独立导入任务。
- Excel 首个工作表字段、必填项、重复字段、空值、期间和金额类型校验。
- 导入问题查询及任务级留痕接口，当前为内存适配器。
- 基础配置导入任务入口。
- 映射关系新增、更新、查询、失效接口。
- 指标库查询和维护接口。
- Excel 文件留存和标准 PnlFact 事实解析。
- 校验通过的 Excel 事实在 `db` Profile 下按批次、视角和任务写入 `raw.pnl_fact`，重复校验采用任务级替换。
- 实际、预算及实际－预算 GAP 的损益计算骨架。
- V001～V008 数据库迁移脚本。

### 3. 已完成版本

| 版本 | 主要内容 |
|---|---|
| `v0.4.0` | 后端基础结构、批次、三视角、经营总览 API |
| `v0.4.1` | 月度导入任务和问题表 |
| `v0.4.2` | Excel 字段级预校验 |
| `v0.5.0` | 配置导入、映射、指标库、损益计算骨架 |
| `v0.5.1` | 已校验 Excel 转标准事实并参与计算 |
| `v0.5.2` | 预算、实际、GAP 计算 |
| `v0.5.8`～`v0.5.17` | 指标树、文件/问题存储、结果/配置/权限/事实/AI JDBC持久化、PVM成本因素、写接口认证及结果时间戳 |

## 二、待开发清单

### P0：上线前必须完成

| 编号 | 工作项 | 当前状态 | 交付结果 |
|---|---|---|---|
| P0-01 | 数据库持久化适配 | 核心仓储及标准事实写入已完成 | `db` Profile 已提供批次、月度/配置导入任务、导入问题、指标库、映射、用户范围、损益结果、AI草稿和标准事实读写 JDBC 仓储；审计仓储仍待补齐 |
| P0-02 | 文件存储适配 | 基础适配已完成 | `filesystem` Profile 下导入文件和问题清单落受控目录并可重启读取；对象存储、保留周期、病毒扫描和清理策略仍待接入 |
| P0-03 | 月度批次完整流程 | 完整性检查已完成 | 创建批次→分视角上传→数据集完整性检查→未匹配处理→开始计算→结果确认→发布；`GET /api/v1/batches/{batchNo}/readiness` 已返回必需数据集、任务状态、问题数和可计算判断 |
| P0-04 | 业务数据导入落库 | 标准事实写入骨架已完成 | 校验通过的任务可由共用 Excel 标准化器写入 `raw.pnl_fact`；期间费用细项、汇率及三视角正式字段仍待样表规则落地 |
| P0-05 | 三视角正式计算 | 部分完成 | 严格按基地、产品线、事业部样表分别计算损益，不能只复用当前通用骨架 |
| P0-06 | 基地损益正式口径 | 未完成 | 基地费用、闲置费用、材料损耗、资产减值、其他收益、B品/零售、两种基地粒度及汇率转换 |
| P0-07 | 产品线/事业部正式口径 | 未完成 | 2.*输入计算1.*结果，独立损益、研发费用映射、减值结果导入、B品统一归类、PVM独立逻辑 |
| P0-08 | 预算/实际/累计 | 部分完成 | 当期、当年1月至所选月累计、年度预算、上月及环比；处理无预算/无实际/电量为0等正常情况 |
| P0-09 | 权限服务端实现 | 部分完成 | 已完成用户范围模型、结果按认证主体过滤、草稿/发布可见性控制及写接口操作人保护；仍需接入公司统一认证、管理员角色授权、菜单权限和配置导入 |
| P0-10 | 正式结果和发布 | 部分完成 | 已实现计算结果版本、计算/确认/发布时间、确认、发布、发布后禁止重算及失败状态；`db` Profile 可持久化结果及明细，数据库历史版本、审计和回退仍待完成 |
| P0-11 | 前后端接口联调 | 核心工作台及批次上传已接入 | Vue 工作台已接入经营总览、损益、PVM、指标树、AI 查询/生成和数据批次创建/上传/校验接口，并提供 API 不可用时的脱敏回退；配置、无权页面和完整错误码联调仍待完成 |

### P1：核心分析能力

| 编号 | 工作项 | 当前状态 | 交付结果 |
|---|---|---|---|
| P1-01 | 经营总览 | Demo已完成，后端未完成 | 三视角共用指标和格式，总部财经可配置指标，按权限返回真实值 |
| P1-02 | 损益结果 | Demo已完成，后端部分完成 | 按财经报表层级、展开关系、预算/实际/GAP、完成率、环比、备注展示 |
| P1-03 | 指标树 | 基础接口已完成 | 已按认证主体读取启用指标库并计算实际、预算、GAP及AI简析模板；复杂公式、来源追溯和下钻仍待增强 |
| P1-04 | 基地 PVM | 细项计算已接入 | 已输出电量/收入/成本/毛利 GAP、量价影响及材料/人工/外协/变动制费/固定制费影响；不同基地粒度、正式样表字段和筛选下钻仍待完善 |
| P1-05 | 产品线/事业部分析 | PVM基础接口已完成 | 已按视角返回独立PVM逻辑标识；产品线与事业部正式损益、项目/型号/客户组合分析仍待样表规则落地 |
| P1-06 | 毛利分析 | Demo已完成，后端未完成 | 分析维度多选，预算/实际/差额/收入占比/毛利及项目小计 |
| P1-07 | 映射配置中心 | 部分完成 | 销售项目、库存、费用分类、费用分摊、汇率、基地粒度等增量维护和失效 |
| P1-08 | 配置导入 | 部分完成 | 基础配置与月度数据入口分离；支持模板校验、重复/冲突、导入结果和问题下载；问题持久化已有 filesystem 适配 |
| P1-09 | 未匹配与人工调整 | Demo已演示，后端未完成 | 财经查看未匹配明细，可修正或忽略；忽略必须留痕且不静默丢失金额 |

### P1：本地 AI 与治理

| 编号 | 工作项 | 当前状态 | 交付结果 |
|---|---|---|---|
| P1-10 | 本地 AI 网关 | 未完成 | 接入公司本地 AI，完成超时、失败、重试、脱敏和权限范围控制 |
| P1-11 | AI 分析草稿 | 基础接口已完成 | 已实现固定六段草稿生成接口，当前为确定性规则Gateway，待替换公司本地AI |
| P1-12 | AI 审核发布 | 基础接口已完成 | 已实现草稿查询和财经角色发布；编辑、基地范围草稿及正式认证仍待完善 |
| P1-13 | AI 追溯 | 基础持久化已完成 | `db` Profile 已提供 AI 草稿及六段内容持久化，绑定批次结果版本；提示词版本、模型编码、审计和正式本地模型调用仍待完善 |
| P1-14 | 数据质量报告 | 未完成 | 完整性、未匹配、排除金额、重复键、映射冲突、勾稽差异和处理状态 |

### P2：上线后增强

- 数据库结果查询优化、缓存和异步计算。
- PVM 多维组合筛选和高性能下钻。
- 通用指标拖拽配置和分析维度自助组合。
- 期间费用、材料损耗、物流费等复杂线下规则的系统化配置。
- ERP、预算、FMS、PMS 等系统接口接入。
- AI 根因分析、趋势预测、行动项跟踪和闭环反馈。
- 导出模板、订阅推送、定时发布和消息通知。
- 复杂多归属库存、比例分摊和历史重算影响分析。

## 三、当前最关键的五个阻断点

1. **数据库和文件持久化仍未达到生产闭环**：核心仓储和标准事实读写已有，但审计、对象存储、保留策略和数据库运行验证仍缺失。
2. **三视角正式公式尚未全部落地**：当前已能算通统一骨架，但还需逐项对照基地、产品线、事业部样表。
3. **真实模板全字段适配尚未完成**：当前解析器支持通用字段别名，仍需用脱敏样表逐字段回归。
4. **统一认证和管理员权限尚未接入**：结果范围过滤和写接口操作人保护已有，仍不能以 Demo 角色切换替代公司统一认证。
5. **发布和AI流程尚未形成完整闭环**：结果版本、确认发布、AI草稿及基础持久化已有，提示词/模型追溯、审计和正式本地模型接入仍待完成。

补充：结果查询已不再接受前端传入工号，必须由容器认证主体提供 `Principal`；在公司统一认证接入前，匿名请求会被拒绝。

## 四、建议开发顺序

1. 先完成数据库、文件存储和导入问题持久化。
2. 用脱敏样表完成三视角字段映射和计算回归。
3. 落地结果版本、发布、权限和审计。
4. 将经营总览、损益、指标树、PVM和毛利分析接入正式 API。
5. 最后接入本地 AI 草稿生成和发布流程。
6. 由财经用真实数据进行 UAT，使用控制总额验证收入、成本、毛利、净利和PVM勾稽差异为0。

## 五、上线判断

当前处于“后端计算链路已打通、生产化能力未完成”阶段。  
可以继续开发，但暂不具备直接上线条件；必须先完成 P0-01、P0-02、P0-05、P0-09、P0-10、P0-11，再进入真实数据 UAT。
